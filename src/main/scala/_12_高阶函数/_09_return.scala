package _12_高阶函数

object _09_return extends App{
/*
  def indexof(str:String, ch:Char) = {
    var i = 0
     until(i == str.length ) {
      if(str(i) == ch ) return i //主动return
      i += 1
    }
    return -1
  }
  */
}
